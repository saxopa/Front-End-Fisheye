const image = document.getElementsByClassName("card-img-top");
export const lightbox = (media) => {
    windows.alert(JSON.stringify(media))
   `
    <div id="modal">
        <p>aaa${media.media.title} </p>
    </div>`;

};


